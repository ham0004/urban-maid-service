{
  "name": "urban-maid-service",
  "version": "1.0.0",
  "description": "A comprehensive web-based platform connecting customers with verified domestic service providers",
  "main": "server/server.js",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "test": "cd server && npm test"
  },
  "keywords": [
    "maid-service",
    "booking-system",
    "react",
    "nodejs",
    "express",
    "mongodb"
  ],
  "author": "Group 10 - CSE471",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}